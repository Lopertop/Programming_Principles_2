# re.ASCII or re.A - Returns only ASCII matches
# re.DEBUG - Returns debug information
# re.DOTALL or re.S - Makes the . character match all characters(including newline)
# re.IGNORECASE or re.I - Case-insensitive matching
# re.MULTILINE or re.M - Returns only matches at the beginning of each line
# re.NOFLAG - Specifies that no flag is set for this pattern
# re.UNICODE or re.U - Returns Unicode matches
# re.VERBOSE or re.X - Allows whitespaces and comments inside paterns.
